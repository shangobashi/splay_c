# Database
DATABASE_URL=postgresql://splay:splay_dev_pass@localhost:5432/splay_dev
POSTGRES_USER=splay
POSTGRES_PASSWORD=splay_dev_pass
POSTGRES_DB=splay_dev

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Storage
STORAGE_TYPE=local
STORAGE_PATH=./storage
S3_BUCKET=splay-uploads
S3_REGION=us-east-1

# API
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
ENVIRONMENT=development

# External Services (Stubbed for MVP)
OPENAI_API_KEY=stub-key-not-used
STRIPE_SECRET_KEY=stub-key-not-used
STRIPE_PUBLISHABLE_KEY=stub-key-not-used
